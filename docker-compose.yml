version: '3'
services:

  rundeck:
    image: "rundeck/rundeck:latest"
    ports:
      - "4440:4440"
#    volumes:
#      - "/var/lib/rundeck:/home/rundeck/server/data"
#    hostname: rundeck.exemple.com

